.po 1i
.TH RMDEL 1 Bell
.V 08/15/84 17:02:43
.\" @[$]rmdel.1	1.6 08/15/84 17:02:43 - Zilog Inc.
.ad
.fi
.SH NAME
rmdel \- remove a delta from an \s-1SCCS\s+1 file
.SH SYNOPSIS
.B rmdel
\fB\-r\fISID files\fR
.SH DESCRIPTION
.B Rmdel\^
removes the delta specified by the \fISID\fR
from each named SCCS file.
The delta to be removed must be the newest (most recent) delta
in its branch in the delta chain of each named SCCS file.
In addition, the specified delta must
.I not\^
be that of a version being edited for the purpose of
making a delta (i.e., if a p-file (see
.BR get (1))
exists for the named SCCS file, the specified delta must
.I not\^
appear in any entry of the
p-file).
.PP
If a directory is named,
.B rmdel\^
behaves as though each file in the directory were specified
as a named file, except that non-SCCS files
(last component of the pathname does not begin with \fBs.\fR)
and unreadable files are silently ignored.
If a name of \fB\-\fR is given, the standard input is read;
each line of the standard input is taken to be the name of an SCCS
file to be processed;
non-SCCS files and unreadable files are silently ignored.
.PP
The exact permissions necessary to remove a delta
are documented in the
.IR "Source Code Control System User's Guide" .
Simply stated, they are either (1) if you make a delta
you can remove it;  or (2) if you own the file and
directory you can remove a delta.
.SH FILES
.PD 0
.TP 10
x-file
(see delta (1))
.TP 10
z-file
(see delta (1))
.PD
.SH SEE ALSO
delta(1),
get(1),
help(1),
prs(1),
sccsfile(5).
.br
.I "Source Code Control System User's Guide\^"
in the ZEUS Utilities Manual.
.br
.SH DIAGNOSTICS
Use help (1) for explanations.
